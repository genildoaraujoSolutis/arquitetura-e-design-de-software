package arquitetura_design_software.solid_chatgpt.srp.correto;

// Classes separadas para cada responsabilidade
class LeitorArquivo {
    public String lerArquivo(String nomeArquivo) {
        // Lógica para ler o arquivo
        return "Conteúdo do arquivo: " + nomeArquivo;
    }
}
